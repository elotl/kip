# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.pyc

# editor backup data
.\#*

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

/virtual-kubelet
/kip
cmd/virtual-kubelet/virtual-kubelet
cmd/kip/kip
/milpactl
/kipctl
staticcheck.conf
kubeconfig
*.tfstate.*
*.tfvars
deploy/terraform/.terraform/
deploy/terraform-gcp/.terraform/
deploy/manifests/virtual-kubelet/overlays/local-*
deploy/manifests/kip/overlays/local-*
deploy/terraform-vpn/kustomization/aws-vpn-client.env
deploy/terraform-vpn/kustomization/*.yaml
