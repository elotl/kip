apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: virtual-kubelet
  namespace: kube-system
spec:
  template:
    spec:
      containers:
        name: virtual-kubelet
        envFrom:
        - secretRef:
            name: virtual-kubelet-secrets
